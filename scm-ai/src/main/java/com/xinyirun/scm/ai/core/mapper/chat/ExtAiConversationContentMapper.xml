<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinyirun.scm.ai.core.mapper.chat.ExtAiConversationContentMapper">

    <select id="selectLastByConversationIdByLimit" resultType="com.xinyirun.scm.ai.bean.domain.AiConversationContent">
        select id, conversation_id, type, content, create_time, model_source_id
        from ai_conversation_content
        where conversation_id = #{conversationId}
        order by create_time desc
        limit #{limit}
    </select>

</mapper>