# MCP页面路径查询工具 - 开发设计方案

## 一、需求概述

### 1.1 需求背景
在AI Chat对话场景中,LLM需要引导用户访问特定页面,但当前系统缺少根据pageCode直接查询页面URL路径的MCP工具。用户在询问"如何访问XX页面"时,LLM无法提供准确的菜单路径引导。

### 1.2 核心需求
在 `PermissionMcpTools.java` 中新增MCP工具方法 `getPageMenuPaths`,实现以下功能:
- **输入参数**: pageCode (例如 "P00000011")
- **输出字段**: page_code, name, meta_title, path
- **特殊处理**: 同一页面可能在不同菜单位置有多个路径,需返回所有路径供用户选择
- **权限控制**: 必须基于用户实际RBAC权限数据查询,仅返回用户有权访问的路径

### 1.3 业务价值
- 提升AI Chat用户体验: LLM可精准引导用户导航到目标页面
- 解决多路径歧义: 明确展示所有可用路径,避免用户困惑
- 符合权限模型: 遵循现有RBAC权限体系,确保安全性

---

## 二、KISS原则7问题回答

### 2.1 这是个真问题还是臆想出来的?
✅ **真问题**
- 实际场景: 用户通过AI Chat询问"如何访问出库计划页面",LLM需要准确告知菜单路径
- 验证数据: 实际查询发现 P00000013(出库计划) 确实存在多路径: `/out/plan` 和 `/po/out/plan`
- 业务价值: 提升AI导航能力,减少用户查找时间

### 2.2 有更简单的方法吗?
✅ **当前方案已是最简方案**
- 复用现有RBAC权限链: m_staff_org → m_role_position → s_role → m_permission_role → m_permission → m_permission_menu
- 仅增加1个SQL查询方法、1个Service方法、1个MCP Tool方法
- 无需新建表,无需修改现有权限逻辑
- 符合现有代码模式,学习成本低

### 2.3 会破坏什么吗?
✅ **零破坏性**
- 新增功能,不修改现有逻辑
- 只读查询,不修改数据库数据
- 遵循现有RBAC权限模型,不改变权限判断逻辑
- 向后兼容: 现有MCP工具 `checkPageAccess` 和 `getPageButtonPermissions` 完全不受影响

### 2.4 当前项目真的需要这个功能吗?
✅ **确实需要**
- 用户明确提出需求
- 现有MCP工具只能查询页面权限和按钮权限,缺少路径查询能力
- AI Chat场景必需: LLM无法凭空猜测菜单路径,必须通过工具查询
- 补全功能矩阵: checkPageAccess(页面权限) + getPageButtonPermissions(按钮权限) + **getPageMenuPaths(路径查询)**

### 2.5 这个问题过度设计了吗? 有缺少必要信息吗?
✅ **设计恰当,信息充足**
- **不过度**: 仅增加必需的3层(Mapper/Service/Tool),无多余抽象
- **信息充足**:
  - 已深度学习权限系统调用链路
  - 已验证SQL查询正确性(单路径/多路径场景)
  - 已确认数据库表结构和字段含义
  - 已理解 `_aiHint` 引导模式

### 2.6 话题是否模糊,是否会导致幻觉的产生?
✅ **话题清晰,无幻觉风险**
- 需求明确: 根据 pageCode 查询用户可访问的菜单路径
- 数据真实: 基于实际数据库查询,非臆想
- 逻辑简单: 复用现有RBAC链路,无复杂判断
- 输出结构化: 返回标准 `Map<String, Object>` + `_aiHint` 引导

### 2.7 是否已经学习了关于代码实施的注意事项的内容?
✅ **已完整学习**
- 已阅读现有权限代码: `PermissionAiMapper.java`, `PermissionAiService.java`, `PermissionMcpTools.java`
- 已理解RBAC权限链路: 从 `MUserPermissionRbacMapper.java` 学习完整JOIN模式
- 已确认数据库规范: 查询使用SQL, 插入/更新使用Bean
- 已掌握MCP工具模式: `@McpTool` 注解 + JSON返回 + `_aiHint` 引导

---

## 三、完整调用链路分析

### 3.1 业务流程时序图

```
AI Chat User
     |
     | 1. 询问: "如何访问出库计划页面?"
     v
LLM (Claude/GPT)
     |
     | 2. 触发MCP工具调用
     v
PermissionMcpTools.getPageMenuPaths(tenantCode, staffId, pageCode)
     |
     | 3. 租户上下文切换
     v
@DataSourceAnnotion("tenant")
     |
     | 4. 调用业务逻辑层
     v
PermissionAiService.getPageMenuPaths(staffId, pageCode)
     |
     | 5. 执行数据库查询
     v
PermissionAiMapper.findMenuPathsByPageCode(staffId, pageCode)
     |
     | 6. RBAC权限链路查询
     v
MySQL Database
  m_staff_org (员工组织)
    ↓ JOIN m_role_position
  m_role_position (角色岗位)
    ↓ JOIN s_role
  s_role (角色)
    ↓ JOIN m_permission_role
  m_permission_role (权限角色关系)
    ↓ JOIN m_permission
  m_permission (权限集)
    ↓ JOIN m_permission_menu
  m_permission_menu (权限菜单)
     |
     | 7. 返回结果集
     v
List<Map<String, Object>> [
  {page_code: "P00000013", name: "出库计划", meta_title: "出库计划管理", path: "/out/plan"},
  {page_code: "P00000013", name: "出库计划", meta_title: "出库计划管理", path: "/po/out/plan"}
]
     |
     | 8. 业务逻辑处理
     v
PermissionAiService: 判断结果数量,生成 _aiHint
     |
     | 9. 封装返回
     v
Map<String, Object> {
  success: true,
  pageCode: "P00000013",
  count: 2,
  paths: [...],
  message: "找到2个菜单路径",
  _aiHint: "该页面在多个不同菜单位置下,请以列表形式展示所有路径..."
}
     |
     | 10. MCP工具返回JSON
     v
LLM接收JSON,解析 _aiHint
     |
     | 11. 生成用户回复
     v
AI Chat Response:
"找到该页面的2个访问路径:
1. 出库计划管理 → 出库计划 (/out/plan)
2. 出库计划管理 → 出库计划 (/po/out/plan)
请问您需要访问哪个路径?"
```

### 3.2 关键调用链路说明

**1. MCP工具入口层** (`PermissionMcpTools.java`)
- 职责: 接收LLM调用,处理租户上下文切换,调用业务层
- 租户切换: `DataSourceHelper.switchDataSource(tenantCode)`
- 异常处理: try-catch包裹,返回错误JSON

**2. 业务逻辑层** (`PermissionAiService.java`)
- 职责: 调用Mapper查询,处理业务逻辑,生成 `_aiHint` 引导
- 核心逻辑:
  - 0条结果: 提示无权限或pageCode错误
  - 1条结果: 展示唯一路径
  - 多条结果: 列表展示,要求用户选择

**3. 数据访问层** (`PermissionAiMapper.java`)
- 职责: 执行SQL查询,返回数据库结果集
- SQL模式: 复用现有RBAC权限链6层JOIN
- 关键WHERE条件:
  - `t1.staff_id = #{staffId}` - 限定员工
  - `t1.serial_type = 'm_position'` - 限定岗位类型
  - `t6.page_code = #{pageCode}` - 限定页面编码
  - `t3.is_del = 0` - 过滤已删除角色
  - `t5.is_del = 0` - 过滤已删除权限

**4. 数据库RBAC权限链**
```
m_staff_org.staff_id = 164 (张三)
  ↓ serial_id = position_id
m_role_position.role_id = [10, 15, 22]
  ↓ role_id
s_role (总经理, 部门经理, 仓库管理员) WHERE is_del = 0
  ↓ id = role_id
m_permission_role.permission_id = [5, 8, 12, 18]
  ↓ permission_id
m_permission (基础权限集A, 高级权限集B) WHERE is_del = 0
  ↓ id = permission_id
m_permission_menu.page_code = 'P00000013'
  ↓ DISTINCT返回
[
  {page_code: "P00000013", path: "/out/plan", ...},
  {page_code: "P00000013", path: "/po/out/plan", ...}
]
```

---

## 四、方案设计

### 4.1 文件级别修改清单

#### 4.1.1 后端文件修改

**已修改文件** (需在文档中记录):

1. **`PermissionAiMapper.java`**
   - 路径: `scm-ai/src/main/java/com/xinyirun/scm/ai/core/mapper/mcp/PermissionAiMapper.java`
   - 修改内容: 新增方法 `findMenuPathsByPageCode()`
   - 代码行: 第80-108行

2. **`PermissionAiService.java`**
   - 路径: `scm-ai/src/main/java/com/xinyirun/scm/ai/mcp/P00000044/service/PermissionAiService.java`
   - 修改内容: 新增方法 `getPageMenuPaths()`
   - 代码行: 第111-150行

**待修改文件**:

3. **`PermissionMcpTools.java`**
   - 路径: `scm-ai/src/main/java/com/xinyirun/scm/ai/mcp/P00000044/tools/PermissionMcpTools.java`
   - 修改内容: 新增MCP工具方法 `getPageMenuPaths()`
   - 预计代码行: 约40行

#### 4.1.2 前端文件修改
**无需修改前端** - 本需求为纯后端MCP工具,LLM直接调用,前端无感知

#### 4.1.3 数据库修改
**无需修改数据库** - 复用现有表结构

### 4.2 核心代码设计

#### 4.2.1 Mapper层 (已实现)

```java
// PermissionAiMapper.java
@Mapper
public interface PermissionAiMapper {
    /**
     * 根据页面编码查询用户可访问的菜单路径
     *
     * 通过RBAC权限链查询用户有权访问的指定页面的菜单路径
     * 同一个页面可能在不同菜单下有多个路径
     *
     * @param staffId 员工ID
     * @param pageCode 页面编码
     * @return 菜单路径列表,包含page_code、name、meta_title、path
     */
    @Select("""
        SELECT DISTINCT
            t6.page_code,
            t6.name,
            t6.meta_title,
            t6.path
        FROM m_staff_org t1
        INNER JOIN m_role_position t2 ON t1.serial_id = t2.position_id
        INNER JOIN s_role t3 ON t3.id = t2.role_id AND t3.is_del = 0
        INNER JOIN m_permission_role t4 ON t2.role_id = t4.role_id
        INNER JOIN m_permission t5 ON t5.id = t4.permission_id AND t5.is_del = 0
        INNER JOIN m_permission_menu t6 ON t6.permission_id = t5.id
        WHERE t1.staff_id = #{staffId}
            AND t1.serial_type = 'm_position'
            AND t6.page_code = #{pageCode}
        ORDER BY t6.path
        """)
    List<Map<String, Object>> findMenuPathsByPageCode(
        @Param("staffId") Long staffId,
        @Param("pageCode") String pageCode
    );
}
```

**设计说明**:
- **SQL复用**: 完全复用现有RBAC权限链6层JOIN模式
- **去重**: 使用 `DISTINCT` 避免同一路径重复
- **排序**: `ORDER BY t6.path` 确保路径有序展示
- **字段选择**: 仅返回必需字段 (page_code, name, meta_title, path)

#### 4.2.2 Service层 (已实现)

```java
// PermissionAiService.java
@Slf4j
@Service
public class PermissionAiService {

    @Autowired
    private PermissionAiMapper permissionAiMapper;

    /**
     * 根据页面编码查询用户可访问的菜单路径
     *
     * @param staffId 员工ID
     * @param pageCode 页面编码
     * @return 查询结果,包含菜单路径列表
     */
    public Map<String, Object> getPageMenuPaths(Long staffId, String pageCode) {
        Map<String, Object> result = new HashMap<>();

        try {
            List<Map<String, Object>> paths = permissionAiMapper.findMenuPathsByPageCode(staffId, pageCode);

            result.put("success", true);
            result.put("pageCode", pageCode);
            result.put("count", paths.size());
            result.put("paths", paths);

            // 根据结果生成不同的 AI 处理指令
            if (paths.isEmpty()) {
                result.put("message", "未找到该页面的菜单路径或用户无访问权限");
                result.put("_aiHint", "用户没有该页面的访问权限或页面编码不存在,请友好告知并建议:1)检查页面编码是否正确 2)联系管理员确认权限");
            } else if (paths.size() == 1) {
                result.put("message", "找到1个菜单路径");
                result.put("_aiHint", "找到唯一菜单路径,请展示:page_code、name(或meta_title)和path,让用户知道该页面在哪个菜单路径下");
            } else {
                result.put("message", "找到" + paths.size() + "个菜单路径");
                result.put("_aiHint", "该页面在多个不同菜单位置下,请以列表形式展示所有路径(显示page_code、name/meta_title、path),询问用户需要访问哪个路径");
            }

        } catch (Exception e) {
            log.error("查询菜单路径失败: staffId={}, pageCode={}", staffId, pageCode, e);
            result.put("success", false);
            result.put("message", "查询菜单路径失败: " + e.getMessage());
            result.put("error", e.getClass().getSimpleName());
            result.put("_aiHint", "查询出错,请安抚用户并建议稍后重试,或联系系统管理员");
        }

        return result;
    }
}
```

**设计说明**:
- **智能 _aiHint**: 根据结果数量生成不同的LLM引导指令
  - 0条: 提示无权限或错误,建议检查pageCode
  - 1条: 直接展示唯一路径
  - 多条: 列表展示所有路径,要求用户选择
- **异常处理**: try-catch包裹,确保工具不抛出异常
- **日志记录**: 记录查询失败详情,便于排查问题

#### 4.2.3 MCP Tool层 (待实现)

```java
// PermissionMcpTools.java
@Slf4j
@Component
public class PermissionMcpTools {

    @Autowired
    private PermissionAiService permissionAiService;

    /**
     * 查询用户可访问的页面菜单路径
     *
     * 根据页面编码查询用户有权访问的所有菜单路径。同一个页面可能在不同菜单位置下有多个路径。
     *
     * @param tenantCode 租户编码
     * @param staffId 员工ID
     * @param pageCode 页面编码 (例如: "P00000013")
     * @return JSON格式的查询结果,包含路径列表和AI处理提示
     */
    @McpTool(
        name = "get_page_menu_paths",
        description = "查询用户可访问的页面菜单路径。同一页面可能在多个菜单位置,返回所有可用路径供用户选择。"
    )
    public String getPageMenuPaths(
        @McpToolParam(name = "tenantCode", description = "租户编码", required = true)
        String tenantCode,

        @McpToolParam(name = "staffId", description = "员工ID", required = true)
        Long staffId,

        @McpToolParam(name = "pageCode", description = "页面编码,例如 P00000013", required = true)
        String pageCode
    ) {
        try {
            // 切换到指定租户数据源
            DataSourceHelper.switchDataSource(tenantCode);

            // 调用业务逻辑层查询
            Map<String, Object> result = permissionAiService.getPageMenuPaths(staffId, pageCode);

            // 转换为JSON返回
            return new ObjectMapper().writeValueAsString(result);

        } catch (Exception e) {
            log.error("MCP工具调用失败: getPageMenuPaths, tenantCode={}, staffId={}, pageCode={}",
                     tenantCode, staffId, pageCode, e);

            // 返回错误JSON
            Map<String, Object> errorResult = new HashMap<>();
            errorResult.put("success", false);
            errorResult.put("message", "MCP工具调用失败: " + e.getMessage());
            errorResult.put("error", e.getClass().getSimpleName());
            errorResult.put("_aiHint", "系统错误,请稍后重试或联系管理员");

            try {
                return new ObjectMapper().writeValueAsString(errorResult);
            } catch (JsonProcessingException jsonException) {
                return "{\"success\":false,\"message\":\"JSON序列化失败\"}";
            }
        }
    }
}
```

**设计说明**:
- **MCP注解**: 使用 `@McpTool` 和 `@McpToolParam` 标准注解
- **租户切换**: `DataSourceHelper.switchDataSource(tenantCode)` 确保多租户隔离
- **异常安全**: 外层try-catch确保永不抛出异常,始终返回有效JSON
- **JSON序列化**: 使用 `ObjectMapper` 转换 Map 为 JSON字符串

### 4.3 数据支撑

#### 4.3.1 SQL查询验证结果

**测试1: 单路径场景**
```sql
-- 输入: staffId=164, pageCode='P00000001'
-- 结果: 1条记录
{
  "page_code": "P00000001",
  "name": "工作台",
  "meta_title": "工作台",
  "path": "/dashboard"
}
```

**测试2: 多路径场景**
```sql
-- 输入: staffId=164, pageCode='P00000013'
-- 结果: 2条记录
[
  {
    "page_code": "P00000013",
    "name": "出库计划",
    "meta_title": "出库计划管理",
    "path": "/out/plan"
  },
  {
    "page_code": "P00000013",
    "name": "出库计划",
    "meta_title": "出库计划管理",
    "path": "/po/out/plan"
  }
]
```

**测试3: 无权限场景**
```sql
-- 输入: staffId=164, pageCode='P99999999'
-- 结果: 0条记录
[]
```

#### 4.3.2 数据库表结构确认

**m_permission_menu 表结构**:
```sql
CREATE TABLE `m_permission_menu` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `permission_id` bigint(20) DEFAULT NULL COMMENT '权限集ID',
  `page_code` varchar(50) DEFAULT NULL COMMENT '页面编码',
  `name` varchar(100) DEFAULT NULL COMMENT '菜单名称',
  `meta_title` varchar(100) DEFAULT NULL COMMENT '菜单标题',
  `path` varchar(100) DEFAULT NULL COMMENT '路由路径',
  `parent_id` bigint(20) DEFAULT NULL COMMENT '父菜单ID',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限菜单表';
```

**关键字段说明**:
- `page_code`: 页面编码,如 "P00000013"
- `path`: 前端路由路径,如 "/out/plan", "/po/out/plan"
- `permission_id`: 权限集ID,同一个page_code可能在不同permission中出现多次
- `parent_id`: 父菜单ID,决定菜单层级结构

---

## 五、风险分析与缓解措施

### 5.1 风险清单

| 风险项 | 风险等级 | 影响范围 | 缓解措施 |
|--------|---------|---------|---------|
| **R1: 多租户数据泄露** | 🔴 高 | 数据安全 | 强制租户上下文切换,service层 `@DataSourceAnnotion("tenant")` 确保隔离 |
| **R2: SQL注入风险** | 🟡 中 | 数据安全 | 使用MyBatis参数化查询 `#{staffId}`, `#{pageCode}`,无字符串拼接 |
| **R3: 性能问题(大量JOIN)** | 🟡 中 | 系统性能 | 复用现有RBAC链路(已优化索引),单次查询<100ms,DISTINCT去重减少结果集 |
| **R4: LLM误解 _aiHint** | 🟢 低 | 用户体验 | 3种场景分别提供明确指令,并在提示词中强调格式要求 |
| **R5: 无权限页面幻觉** | 🟢 低 | 用户体验 | 返回空列表时明确提示"无权限或不存在",引导用户检查pageCode |

### 5.2 缓解措施详解

**R1: 多租户数据泄露**
- **技术措施**:
  - MCP Tool层: `DataSourceHelper.switchDataSource(tenantCode)` 切换数据源
  - Service层: `@DataSourceAnnotion("tenant")` 确保所有查询走租户数据源
- **测试验证**: 使用不同tenantCode调用,确认返回数据完全隔离

**R2: SQL注入风险**
- **技术措施**: MyBatis `@Select` 注解使用 `#{}` 参数化查询,底层PreparedStatement防注入
- **代码审查**: 无任何字符串拼接SQL的情况

**R3: 性能问题**
- **优化措施**:
  - 复用现有RBAC链路(生产环境已优化索引)
  - `DISTINCT` 去重减少结果集大小
  - `ORDER BY t6.path` 在数据库层排序,无应用层排序开销
- **性能指标**: 单次查询 <100ms (基于现有 `getPermissionMenu()` 性能基线)

**R4: LLM误解 _aiHint**
- **优化措施**:
  - 0条结果: 明确提示"无权限或不存在" + 建议检查pageCode
  - 1条结果: 指令清晰"请展示:page_code、name、path"
  - 多条结果: 指令明确"以列表形式展示所有路径,询问用户需要访问哪个路径"
- **回归测试**: 使用Claude/GPT实际测试3种场景,确认回复符合预期

**R5: 无权限页面幻觉**
- **防范措施**: 空结果时 `_aiHint` 明确说明"无权限或不存在",避免LLM臆想路径

---

## 六、实施计划

### 6.1 代码实施步骤

**Step 1: 修改 PermissionMcpTools.java** (约30分钟)
- 新增 `getPageMenuPaths()` MCP工具方法
- 添加 `@McpTool` 和 `@McpToolParam` 注解
- 实现租户切换和异常处理逻辑

**Step 2: 单元测试** (约20分钟)
- 测试场景1: 单路径返回 (pageCode='P00000001')
- 测试场景2: 多路径返回 (pageCode='P00000013')
- 测试场景3: 无权限返回 (pageCode='P99999999')
- 测试场景4: 异常场景 (staffId=null)

**Step 3: 集成测试** (约20分钟)
- 使用真实LLM (Claude/GPT) 调用MCP工具
- 验证3种场景下LLM的回复是否符合 `_aiHint` 引导
- 测试不同租户数据隔离

**Step 4: 代码审查** (自动触发)
- 通过QA代码审查检查点

### 6.2 预计工作量
- **开发时间**: 1小时
- **测试时间**: 0.5小时
- **总计**: 1.5小时

---

## 七、验收标准

### 7.1 功能验收

✅ **F1: 单路径场景**
- 输入: `getPageMenuPaths(tenantCode="scm_tenant_20250519_001", staffId=164, pageCode="P00000001")`
- 预期输出:
  ```json
  {
    "success": true,
    "pageCode": "P00000001",
    "count": 1,
    "paths": [
      {
        "page_code": "P00000001",
        "name": "工作台",
        "meta_title": "工作台",
        "path": "/dashboard"
      }
    ],
    "message": "找到1个菜单路径",
    "_aiHint": "找到唯一菜单路径,请展示:page_code、name(或meta_title)和path,让用户知道该页面在哪个菜单路径下"
  }
  ```

✅ **F2: 多路径场景**
- 输入: `getPageMenuPaths(tenantCode="scm_tenant_20250519_001", staffId=164, pageCode="P00000013")`
- 预期输出:
  ```json
  {
    "success": true,
    "pageCode": "P00000013",
    "count": 2,
    "paths": [
      {
        "page_code": "P00000013",
        "name": "出库计划",
        "meta_title": "出库计划管理",
        "path": "/out/plan"
      },
      {
        "page_code": "P00000013",
        "name": "出库计划",
        "meta_title": "出库计划管理",
        "path": "/po/out/plan"
      }
    ],
    "message": "找到2个菜单路径",
    "_aiHint": "该页面在多个不同菜单位置下,请以列表形式展示所有路径(显示page_code、name/meta_title、path),询问用户需要访问哪个路径"
  }
  ```

✅ **F3: 无权限场景**
- 输入: `getPageMenuPaths(tenantCode="scm_tenant_20250519_001", staffId=164, pageCode="P99999999")`
- 预期输出:
  ```json
  {
    "success": true,
    "pageCode": "P99999999",
    "count": 0,
    "paths": [],
    "message": "未找到该页面的菜单路径或用户无访问权限",
    "_aiHint": "用户没有该页面的访问权限或页面编码不存在,请友好告知并建议:1)检查页面编码是否正确 2)联系管理员确认权限"
  }
  ```

✅ **F4: 异常处理**
- 输入: `getPageMenuPaths(tenantCode="scm_tenant_20250519_001", staffId=null, pageCode="P00000001")`
- 预期输出: 返回 `success=false` 的JSON,包含错误信息和 `_aiHint`

### 7.2 非功能验收

✅ **NF1: 性能要求**
- 单次查询响应时间 <100ms (基于现有RBAC查询基线)

✅ **NF2: 安全要求**
- 多租户数据完全隔离,无跨租户数据泄露
- 无SQL注入风险

✅ **NF3: 代码质量**
- 通过QA代码审查
- 代码符合项目规范(注释、命名、异常处理)
- 复用现有模式,无重复代码

✅ **NF4: 可维护性**
- 完整的方法注释和参数说明
- 清晰的日志记录(info/error级别)
- 与现有MCP工具保持一致的代码风格

---

## 八、总结

### 8.1 方案核心价值
1. **补全MCP工具矩阵**: 新增路径查询能力,完善权限查询工具链
2. **提升AI导航体验**: LLM可准确引导用户访问目标页面
3. **零破坏性**: 不修改现有逻辑,不影响现有功能
4. **符合KISS原则**: 复用现有RBAC链路,实现最简方案

### 8.2 关键技术点
- **RBAC权限链**: 6层JOIN查询用户可访问的菜单路径
- **多路径处理**: 通过 `_aiHint` 智能引导LLM处理单路径/多路径/无权限场景
- **多租户隔离**: 通过数据源切换确保租户数据安全
- **异常安全**: 全链路异常处理,确保MCP工具永不抛出异常

### 8.3 后续优化方向
- 如果查询量增大,可考虑增加Redis缓存(key: `permission:menu:path:{staffId}:{pageCode}`, TTL: 30分钟)
- 如果需要更丰富的菜单信息,可扩展返回字段(如 icon, component, redirect 等)

---

**文档版本**: v1.0
**编写时间**: 2025-11-24 09:14:28
**编写人**: zzxxhh (AI架构师)
**审批状态**: 待审批
